export const CONFIG = {
    // API Configuration
    // Use your computer's LAN IP if testing on physical device (Expo Go)
    // Use 10.0.2.2 for Android Emulator, localhost for iOS Simulator
    // Or use the Production URL for deployed backend
    API_BASE_URL: 'https://fd-dashboard-web.onrender.com/api',

    // Game Constants
    SALARY_CAP: 60000,

    // Defaults
    DEFAULT_YEAR: 2025,
    DEFAULT_WEEK: 16
};
